name: backend-dev

on:
    push:
        branches:
            - main

jobs:
    deploy_function:
        runs-on: ubuntu-latest
        steps:
            - name: Echo the message
              run: |
                echo "hola mundo"

            


